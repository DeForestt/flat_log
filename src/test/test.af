.needs <std>

import string from "String";
import LogLevel from "../Logger";
import {getLogger} from "../Logger" under logger;

int main() {
	logger.getLogger().log(LogLevel.Info, "This is a test");
	return 0;
};
